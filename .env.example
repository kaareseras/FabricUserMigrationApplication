# Copy to .env and adjust. compose.yaml reads these values from the environment.

# Shared secret required on mutating API endpoints (Authorization: Bearer <token>).
# Generate one with:  openssl rand -hex 32
FABRIC_ATLAS_TOKEN=

# Force a snapshot re-import on container start even when the database already exists.
FORCE_SNAPSHOT_IMPORT=false
